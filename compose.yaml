services:
  app:
    build: .
    image: containerized-python-app
    container_name: containerized-python-app

    ports:
      - "${HOST_PORT}:${APP_PORT}"

    environment:
      APP_ENV: ${APP_ENV}
      APP_PORT: ${APP_PORT}